class Cancion {
	constructor(nombre, duracion, genero, album, autor) {
			this.nombre = nombre
			this.duracion = duracion
			this.genero = genero
			this.autor = autor//new Autor(autor.nombre, autor.pais);
	}
}